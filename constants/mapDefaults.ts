import type { LngLat } from '~/types'

/** Default map view when no pins yet — St. John's, Newfoundland and Labrador */
export const DEFAULT_MAP_CENTER: LngLat = {
  lng: -52.7126,
  lat: 47.5615,
}

export const DEFAULT_MAP_ZOOM = 12
